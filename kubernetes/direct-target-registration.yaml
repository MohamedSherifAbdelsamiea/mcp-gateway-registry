apiVersion: v1
kind: Service
metadata:
  name: mcp-gateway-registry-new
  namespace: mcp-gateway
spec:
  selector:
    app: mcp-gateway-registry-new
  ports:
  - port: 80
    targetPort: 7860
    protocol: TCP
  type: NodePort