apiVersion: v1
kind: Service
metadata:
  name: mcp-gateway-registry
  namespace: mcp-registry
  labels:
    app: mcp-gateway-registry
spec:
  selector:
    app: mcp-gateway-registry
  ports:
  - port: 80
    targetPort: 80
    protocol: TCP
    name: http
  type: ClusterIP
