# MCP Gateway Registry CDK Configuration Template
# Copy this file to .env and fill in your actual values

# =============================================================================
# DEPLOYMENT CONFIGURATION
# =============================================================================

# Deployment mode: "complete", "infrastructure-only", or "application-only"
DEPLOYMENT_MODE=application-only

# Domain Configuration
DOMAIN_NAME=mcp.yourdomain.com

# Admin Credentials
ADMIN_USER=admin
ADMIN_PASSWORD=your-secure-admin-password

# AWS Configuration
CDK_DEFAULT_REGION=us-east-1
AWS_PROFILE=your-aws-profile

# =============================================================================
# INFRASTRUCTURE CONFIGURATION (for application-only mode)
# =============================================================================

# EXISTING INFRASTRUCTURE - Required for application-only mode
CLUSTER_NAME=your-eks-cluster-name
EFS_FILE_SYSTEM_ID=fs-xxxxxxxxxxxxxxxxx
CERTIFICATE_ARN=arn:aws:acm:region:account:certificate/cert-id

# =============================================================================
# COGNITO CONFIGURATION
# =============================================================================

# User Pool Configuration (from AWS Cognito Console)
COGNITO_USER_POOL_ID=us-east-1_XXXXXXXXX
COGNITO_CLIENT_ID=your-cognito-client-id
COGNITO_CLIENT_SECRET=your-cognito-client-secret

# M2M Client Configuration (for agent authentication)
COGNITO_M2M_CLIENT_ID=your-m2m-client-id
COGNITO_M2M_CLIENT_SECRET=your-m2m-client-secret

# Cognito Domain (optional - will be auto-generated if not provided)
# COGNITO_DOMAIN=mcp-gateway-your-cluster-name

# =============================================================================
# CERTIFICATE CONFIGURATION (for complete infrastructure mode)
# =============================================================================

# Route53 Configuration (for automatic certificate creation)
HOSTED_ZONE_ID=Z1234567890ABC
CREATE_CERTIFICATE=true

# =============================================================================
# ADVANCED CONFIGURATION
# =============================================================================

# VPC Configuration (for complete infrastructure mode)
VPC_CIDR=10.0.0.0/16
MAX_AZS=3

# EKS Configuration
KUBERNETES_VERSION=1.28

# =============================================================================
# OAUTH CALLBACK URLS (automatically configured by CDK)
# =============================================================================
# The CDK will automatically configure these callback URLs:
# - http://localhost:9090/callback (for CLI user authentication)
# - http://localhost/oauth2/callback/cognito (for testing)
# - http://localhost:8888/oauth2/callback/cognito (for local development)
# - https://your-domain/oauth2/callback/cognito (for production)

# =============================================================================
# USER GROUPS (automatically created by CDK)
# =============================================================================
# The CDK will automatically create these user groups:
# - mcp-registry-admin (for admin users)
# - mcp-registry-user (for regular users)

# =============================================================================
# M2M SCOPES (automatically configured by CDK)
# =============================================================================
# The CDK will automatically configure these M2M scopes:
# - mcp-servers-unrestricted/read (read access to all MCP servers)
# - mcp-servers-unrestricted/execute (execute access to all MCP servers)
